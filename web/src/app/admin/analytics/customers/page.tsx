import { CustomerActivityClient } from './CustomerActivityClient';

export const metadata = { title: 'Customer activity' };

export default function AdminCustomerActivityPage() {
  return <CustomerActivityClient />;
}
